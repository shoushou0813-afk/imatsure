{
  "name": "imatsure",
  "private": true,
  "version": "0.1.0",
  "description": "イマツレ - 釣り掲示板（React + Express + Prisma）",
  "scripts": {
    "setup": "npm --prefix server install && npm --prefix client install && npm run db:setup",
    "db:setup": "npm --prefix server run db:setup",
    "db:reset": "npm --prefix server run db:reset",
    "dev": "concurrently -n server,client -c cyan,magenta \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev"
  },
  "devDependencies": { "concurrently": "^9.1.0" }
}
