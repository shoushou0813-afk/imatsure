# 用語集

このプロジェクトで出てくる言葉を、実際にどこで使われているかとセットで説明します。

## Web／React まわり

**SPA（Single Page Application）**
ページ遷移のたびにHTMLを取り直すのではなく、最初に読み込んだJavaScriptが画面を書き換える方式。
リンクを押しても画面が白くならないのはこれ。React Router がURLと画面の対応を管理しています。

**ルーティング**
「どのURLでどの画面を出すか」の対応表。`client/src/App.jsx` がそれです。

**コンポーネント**
画面の部品。`TripRow.jsx` は「釣況リストの1行」という部品で、データを渡すと同じ形で何度でも描けます。

**props（プロップス）**
コンポーネントに外から渡す値。`<TripRow trip={t} />` の `trip` が props です。

**state（ステート）**
コンポーネントが自分で持つ、変わると画面が描き直される値。`useState` で作ります。
`SpotBoard.jsx` のフィルタ条件がこれ。

**フック（hook）**
`use` で始まる関数。React の機能を関数コンポーネントから使うための仕組み。
`useState`（状態）、`useEffect`（副作用＝通信など）、`useMemo`（計算結果の使い回し）が基本の3つ。

**Context（コンテキスト）**
値をコンポーネントのバケツリレーなしで配る仕組み。`auth.jsx` がログイン状態を配っています。

**副作用（side effect）**
画面を描く以外の処理。通信、タイマー、DOM操作など。`useEffect` の中に書きます。

**プロキシ（vite の proxy）**
開発中、`/api` へのリクエストをVite（5173番）がExpress（4000番）へ転送する設定。
ブラウザからは同じオリジンに見えるので、Cookieがそのまま送れます。

## サーバ／DB まわり

**REST API**
URL（何を）とHTTPメソッド（どうする）でサーバ機能を公開する方式。
GET＝取得、POST＝作成、PATCH＝部分更新、DELETE＝削除。

**ミドルウェア（middleware）**
リクエストが本処理に届く前に通す共通処理。認証チェック、エラー整形、レートリミットなど。
`server/src/middleware/` と `index.js` の `app.use(...)` がそれです。

**ORM**
Object-Relational Mapping。SQLを直接書かず、JavaScriptのオブジェクトとしてDBを扱う道具。ここでは Prisma。

**スキーマ（schema）**
テーブルの構造の定義。`prisma/schema.prisma` がそれ。

**マイグレーション**
スキーマ変更の履歴をコードとして残し、順番に適用する仕組み。
このプロジェクトでは開発を速くするため `prisma db push`（履歴なしで直接反映）を使っています。
本番運用に入る前に `prisma migrate` へ切り替えてください。

**シード（seed）**
開発用の初期データをDBに流し込むこと。空のDBだと画面の見た目が確認できないため必須。

**リレーション（1対多）**
1つの釣り場に複数の釣行がぶら下がる関係。`Spot ||--o{ Trip` と書きます。

**論理削除**
行を実際に消さず `deletedAt` に時刻を入れて「消したことにする」方式。
他人のコメントが宙に浮くのを防げます。

**カーソルページング**
「何ページ目」ではなく「この時刻より前の20件」と指定する方式。
投稿が増え続ける一覧でも、項目の重複・欠落が起きません。`/api/trips?cursor=...` がこれ。

**インデックス（index）**
「この列で検索・並べ替えが多い」とDBに教える印。無いと件数が増えたとき急に遅くなります。
`@@index([spotId, startedAt])` がそれ。

## 認証・セキュリティ

**ハッシュ化（bcrypt）**
パスワードを元に戻せない形に変換して保存すること。DBが漏れても生のパスワードは出ません。

**JWT**
ログイン済みであることを証明する、署名つきの文字列。サーバがセッションを覚えなくて済みます。

**HttpOnly Cookie**
JavaScriptから読めないCookie。トークンをここに入れると、悪意あるスクリプトに盗まれにくくなります（XSS対策）。

**CORS**
違うオリジン（ドメイン・ポート）からのリクエストを許可する仕組み。`index.js` の `cors(...)` がそれ。

**レートリミット**
一定時間に受け付けるリクエスト数の上限。連投・スクリプト投稿を止めます。

**Exif**
写真ファイルに埋め込まれた撮影情報（日時・GPS・カメラ設定）。
自動補完に使える反面、そのまま公開すると自宅が特定されるので削除が必要です。

## デザイン・設計

**デザイントークン**
色や余白に「役割の名前」を付けて変数にしたもの。`--accent`（釣況）、`--regu`（規制・注意）など。
`styles.css` の `:root` にあります。ダークモードはこの変数だけを差し替えて実現しています。

**情報設計（IA）**
どの情報をどの階層・どの順で置くかの設計。「見やすさ」の大半はCSSではなくここで決まります。

**粒度**
データをどれくらい細かく分けて持つか。「1匹単位」か「1釣行単位」か、という選択がこれ。

**WCAG AA**
Webアクセシビリティの国際基準。本文のコントラスト比 4.5:1 以上、などの数値が定められています。

**LCP**
Largest Contentful Paint。一番大きい要素が表示されるまでの時間。体感速度の代表指標で 2.5秒以内が合格。

## 釣り用語

**釣果（ちょうか）** — 釣れた魚のこと、またはその記録。
**釣行（ちょこう）** — 釣りに行くこと1回分。このアプリのデータの単位。
**ボウズ** — 1匹も釣れないこと。記録できるかどうかで統計の正しさが変わります。
**潮汐（ちょうせき）／大潮・小潮** — 月と太陽の引力による海面の上下。大潮は干満差が大きく、一般に釣果が上がりやすいとされます。
**朝マズメ／夕マズメ** — 日の出前後・日没前後の、魚がよく釣れる時間帯。
**タックル** — 竿・リール・糸などの道具構成。
**ショアジギング／エギング／フカセ** — 釣り方の種類。それぞれ青物・イカ・磯魚を狙う釣法。
